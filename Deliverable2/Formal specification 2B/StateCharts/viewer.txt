The viewer class begins in the NOP state, which means that it isn't doing anything, after it receieves an update, it can postpone to update the view but it will always have to update it. After updateing the view it call's the function notifySatechange() to let everyone know the view has been updated.